process PROD = {
	source = PoolSource {
		 untracked vstring fileNames = {'file:segment_eta_09_12.root'}
	         # untracked vstring fileNames = {'file:segment_eta_12_18.root'}
		 # untracked vstring fileNames = {'file:segment_eta_18_24.root'}
     		 untracked int32 maxEvents = 1000
     		 untracked uint32 debugVebosity = 10
     		 untracked bool   debugFlag     = false
		}
 
        include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
        include "Geometry/CSCGeometry/data/cscGeometry.cfi"


	module rechitreader = CSCRecHitReader{
		# Switch on/off the verbosity and turn on/off histogram production
		untracked bool debug = false
		# Label to retrieve SimHits from the event
		untracked string simHitLabel = 'SimG4Object'
		# Label to retrieve RecHits from the event
		untracked string recHitLabel = 'rechitproducer'
		# Name of the root file which will contain the histos
		untracked string rootFileName = "CSCRecHitPlots.root"
	        # The following variables are unused at this point:
	        untracked int32 minRechitPerChamber = 1
	        untracked int32 maxRechitPerChamber = 10


	}	

#	module ana = EventContentAnalyzer {untracked int32 debugLevel = 2}
#	path p = {ana,rechitreader}
	path p = {rechitreader}
}

