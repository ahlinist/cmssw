process MONITOR={

    source = PoolSource {
	untracked vstring fileNames = {'file:alCaPi0.root'}
	untracked int32  maxEvents = -1
    }

    # Configure the Pi0 Calib Monitor
    include "DQM/Calibration/data/pi0ECALOnlineCalibMonitor.cff"
    
    path p ={pi0onlinecalibmonitor}

} 
