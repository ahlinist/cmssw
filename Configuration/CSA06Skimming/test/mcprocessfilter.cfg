 module  skimfilter1  = MCProcessFilter
    {

     # vector of accepted processID's (logical OR)-- if absent all processID are accepted
      untracked vint32 ProcessID = {1,2}

      # vector of min pthat values corresponding to above processID's -- if absent values are 0
      untracked vdouble MinPthat = {40,0}

      # vector of min pthat values corresponding to above processID's -- if absent values are 10000
      untracked vdouble MaxPthat = {50,10000}

 }
