process Demo =  {
  #keep the logging output to a nice level
  include "FWCore/MessageLogger/data/MessageLogger.cfi"

  source = PoolSource {
     untracked vstring fileNames = {"file:/localscratch/s/speer/cmssw/CMSSW_1_2_0_pre9/src/Configuration/ReleaseValidation/data/b_jets.root"}
     untracked int32  maxEvents     = -1
  }

  module btagMCTest = BtagMCTest
  {

  string moduleLabel = "trackCountingJetTags"

  PSet jetIdParameters = {
    string mcSource = "source"
    bool fillPartons = true
    bool fillHeavyHadrons = false
    bool fillLeptons =  false
    double coneSizeToAssociate = 0.3
    bool physicsDefinition = false
    bool rejectBCSplitting = false
    vstring vetoFlavour = {  }
    }
  }


   path p = {btagMCTest} 
}
