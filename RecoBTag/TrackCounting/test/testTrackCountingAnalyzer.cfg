#! /bin/env cmsRun

process analyzer = {

    # input file
    source = PoolSource {
        untracked vstring fileNames = { "file:/afs/cern.ch/cms/Tutorials/july06/BTauReconstruction/bjets_btagtkcnt_1event.root" }
        untracked int32 maxEvents = -1
    }

    # Message Logger
    include "FWCore/MessageLogger/data/MessageLogger.cfi"
    replace MessageLogger.cout.threshold = "INFO"

    # Test tracks to jets association
    module tkcnt = TrackCountingAnalyzer {
    }

    # module execution
    path p = { tkcnt }

}
