#! /bin/env cmsRun

process analyzer = {

    # input file
    source = PoolSource {
        untracked vstring fileNames = { "file://data/arizzi/testPVproblem/CMSSW_1_2_0/src/RecoBTag/TrackCounting/test/btag.root"}
        untracked int32 maxEvents = -1
    }

    # Message Logger
    include "FWCore/MessageLogger/data/MessageLogger.cfi"
    replace MessageLogger.cout.threshold = "INFO"
  

    # Test tracks to jets association
    module tkcnt = TrackCountingAnalyzer {
    }

    # module execution
    path p = { tkcnt }

}
