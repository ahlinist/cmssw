1) PATH/FileName  for event file (PATH is optional)
CompHEP_events.PEV
2) Process string (just for comment in PYTHIA output)
p,p -> p,p
1) First event to be written in ntuple from parton event flow:
1
